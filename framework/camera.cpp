#include "camera.hpp"

Camera::Camera(std::string name, glm::vec3 pos, float fovX,  float resX) :
		name_{name}, position_{pos}, fovX_{fovX}, distance_{0} {
			// todo calculate Distance
			// distance_= 
	};
	
	// glm::vec3 Camera::translate() {};
	// glm::vec3 Camera::rotate() {};
